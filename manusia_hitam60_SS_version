local Players = game:GetService("Players")
local myName = "Your user name"
local targetPlayer = Players:FindFirstChild(myName)
if not targetPlayer then
return
end
local pGui = targetPlayer:FindFirstChildOfClass("PlayerGui")
if not pGui then
local startTime = tick()
while not pGui and tick() - startTime < 10 do
pGui = targetPlayer:FindFirstChildOfClass("PlayerGui")
task.wait(0.2)
end
end
if pGui then
local ID_Main = "rbxassetid://131552532497284"
local ID_Music = "rbxassetid://139328999836171"
if pGui:FindFirstChild("manusia_hitam60 (ultimate ss edition)") then 
pGui["manusia_hitam60 (ultimate ss edition)"]:Destroy() 
end
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "manusia_hitam60 (ultimate ss edition)"
ScreenGui.ResetOnSpawn = false
ScreenGui.DisplayOrder = 99999
ScreenGui.Parent = pGui
local legends = {
["manusia_hitam60"] = "not a friends, but OWNER",
["kevinphone7"] = "THIS SCRIPT DESIGNED FOR YOU",
["V990one_c00lkizz"] = "YOOOOYOU TRY IT MY FRIEND",
["Realoscarina"] = "THANK YOU FOR THE CHAT SCRIPT",
["lookid13"] = "LETS TO TRY IT",
["glitch_du"] = "NOW LETS TRY MY SCRIPT",
["R0van_exploiter"] = "YO ROVAN, TEST MY SCRIPT"
}
local myStatus = legends[targetPlayer.Name] or "USER"
if myStatus ~= "USER" then
local n = Instance.new("Frame", ScreenGui)
n.Size = UDim2.new(0, 300, 0, 60)
n.Position = UDim2.new(1, 10, 0.8, 0)
n.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
n.BorderSizePixel = 2
n.BorderColor3 = Color3.fromRGB(255, 215, 0)
local t = Instance.new("TextLabel", n)
t.Size = UDim2.new(1, -20, 1, 0)
t.Position = UDim2.new(0, 10, 0, 0)
t.BackgroundTransparency = 1
t.Text = "WELCOME THE GREATEST FRIENDS\n[" .. myStatus .. " " .. targetPlayer.Name .. "]"
t.TextColor3 = Color3.new(1,1,1)
t.Font = "Code"
t.TextSize = 14
t.TextXAlignment = "Left"
n:TweenPosition(UDim2.new(1, -320, 0.8, 0), "Out", "Quad", 0.3, true)
task.delay(20, function()
n:TweenPosition(UDim2.new(1, 10, 0.8, 0), "In", "Quad", 0.3, true)
task.wait(20)
n:Destroy()
end)
end
ScreenGui.DisplayOrder = 999999
local MainFrame = Instance.new("Frame", ScreenGui)
local ToggleGui = Instance.new("ScreenGui", pGui)
ToggleGui.Name = "AbilToggleSystem"
ToggleGui.DisplayOrder = 1000 
ToggleGui.ResetOnSpawn = false
local ToggleBtn = Instance.new("ImageButton", ToggleGui)
ToggleBtn.Size = UDim2.new(0, 40, 0, 40)
ToggleBtn.Position = UDim2.new(0, 5, 0.5, -20) 
ToggleBtn.Image = "rbxassetid://71983226245811" 
ToggleBtn.Draggable = true ToggleBtn.Active = true
Instance.new("UICorner", ToggleBtn)
ToggleBtn.MouseButton1Click:Connect(function()
if MainFrame then
MainFrame.Visible = not MainFrame.Visible 
end
end)
MainFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 0)
MainFrame.Size = UDim2.new(0, 420, 0, 300)
MainFrame.Position = UDim2.new(0.5, -210, 0.5, -135)
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 15)
MainFrame.Visible = true    
local MusicBtn = Instance.new("TextButton", MainFrame)
MusicBtn.Size = UDim2.new(0, 50, 0, 25)
MusicBtn.Position = UDim2.new(1, -60, 0, 5)
MusicBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
MusicBtn.Text = "Music"
MusicBtn.TextColor3 = Color3.new(1,1,1)
MusicBtn.Font = "SourceSansBold"
MusicBtn.TextSize = 12
Instance.new("UICorner", MusicBtn)
local StopMusicBtn = Instance.new("TextButton", MainFrame)
StopMusicBtn.Size = UDim2.new(0, 50, 0, 25)
StopMusicBtn.Position = UDim2.new(1, -115, 0, 5)
StopMusicBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
StopMusicBtn.Text = "Stop"
StopMusicBtn.TextColor3 = Color3.new(1,1,1)
StopMusicBtn.Font = "SourceSansBold"
StopMusicBtn.TextSize = 12
Instance.new("UICorner", StopMusicBtn)
local isPlaying = false
MusicBtn.MouseButton1Click:Connect(function()
if workspace:FindFirstChild("MH60_GLOBAL_BGM") then
workspace.MH60_GLOBAL_BGM:Destroy()
end
isPlaying = true
MusicBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
local bgm = Instance.new("Sound", workspace)
bgm.Name = "MH60_GLOBAL_BGM"
bgm.SoundId = ID_Music
bgm.Looped = true
bgm.Volume = 100000
bgm.PlaybackSpeed = 0.07
bgm:Play()
end)
StopMusicBtn.MouseButton1Click:Connect(function()
isPlaying = false
MusicBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
for i, v in next, game:GetDescendants() do
if v:IsA("Sound") then
v:Stop()
if v.Name == "MH60_GLOBAL_BGM" then
v:Destroy()
end
end
end
end)
local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 35)
Title.Text = "  MANUSIA_HITAM60 (ULTIMATE SS EDITION)"
Title.TextColor3 = Color3.new(0,0,0)
Title.BackgroundTransparency = 1 Title.TextSize = 16
Title.Font = "SourceSansBold" Title.TextXAlignment = "Left"
local ButtonGrid = Instance.new("Frame", MainFrame)
ButtonGrid.BackgroundTransparency = 1
ButtonGrid.Position = UDim2.new(0, 10, 0, 45)
ButtonGrid.Size = UDim2.new(1, -20, 1, -55)
local Grid = Instance.new("UIGridLayout", ButtonGrid)
Grid.CellSize = UDim2.new(0, 75, 0, 34)
Grid.CellPadding = UDim2.new(0, 6, 0, 6)
Grid.HorizontalAlignment = Enum.HorizontalAlignment.Center
local function makeBtn(name, color, func)
local btn = Instance.new("TextButton", ButtonGrid)
btn.Text = name btn.BackgroundColor3 = color btn.TextColor3 = Color3.new(1,1,1)
btn.Font = "SourceSansBold" btn.TextSize = 9 btn.TextWrapped = true
Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
btn.MouseButton1Click:Connect(func)
end
local blue, green, black = Color3.fromRGB(0, 170, 255), Color3.fromRGB(85, 170, 0), Color3.fromRGB(0, 0, 0)
local skyActive = false
makeBtn("Skybox", blue, function()
local Lighting = game:GetService("Lighting")
if skyActive then
skyActive = false
for _, v in pairs(Lighting:GetChildren()) do
if v.Name == "MH60_Sky" then v:Destroy() end
end
return
end
skyActive = true
for _, v in next, Lighting:GetChildren() do
if v:IsA("Sky") or v:IsA("Atmosphere") or v:IsA("Clouds") then v:Destroy() end
end
local newSky = Instance.new("Sky", Lighting)
newSky.Name = "MH60_Sky"
newSky.SkyboxBk = ID_Main; newSky.SkyboxDn = ID_Main; newSky.SkyboxFt = ID_Main
newSky.SkyboxLf = ID_Main; newSky.SkyboxRt = ID_Main; newSky.SkyboxUp = ID_Main
end)
local decalActive = false
makeBtn("Decal Spam", blue, function()
if decalActive then
decalActive = false
for _, v in pairs(workspace:GetDescendants()) do
if v.Name == "MH60_SpamDecal" then v:Destroy() end
end
return
end
decalActive = true
local faces = {"Front","Back","Top","Bottom","Left","Right"}
for _, v in pairs(workspace:GetDescendants()) do 
if v:IsA("BasePart") then 
for _, f in pairs(faces) do 
local d = Instance.new("Decal", v) 
d.Name = "MH60_SpamDecal"
d.Texture = ID_Main 
d.Face = f 
end 
end 
end
end)
local particleActive = false
makeBtn("Particle", blue, function()
if particleActive then
particleActive = false
for _, v in pairs(workspace:GetDescendants()) do
if v.Name == "MH60_GlobalAttachment" or v.Name == "MH60_Emitter" then
v:Destroy()
end
end
return
end
particleActive = true
local function applyToPart(part)
if part:IsA("BasePart") and not part:FindFirstChild("MH60_GlobalAttachment") then
local attachment = Instance.new("Attachment", part)
attachment.Name = "MH60_GlobalAttachment"
local emitter = Instance.new("ParticleEmitter", attachment)
emitter.Name = "MH60_Emitter"
emitter.Texture = ID_Main
emitter.Rate = 100
emitter.Lifetime = NumberRange.new(5, 10)
emitter.Speed = NumberRange.new(10, 20)
emitter.Size = NumberSequence.new(3)
local randomFaces = {Enum.NormalId.Top, Enum.NormalId.Bottom, Enum.NormalId.Left, Enum.NormalId.Right, Enum.NormalId.Front, Enum.NormalId.Back}
emitter.EmissionDirection = randomFaces[math.random(1, #randomFaces)]
emitter.SpreadAngle = Vector2.new(180, 180)
emitter.Acceleration = Vector3.new(math.random(-5,5), math.random(-5,5), math.random(-5,5))
end
end
for _, obj in pairs(workspace:GetDescendants()) do
applyToPart(obj)
end
local connection
connection = workspace.DescendantAdded:Connect(function(newObj)
if not particleActive then connection:Disconnect() return end
applyToPart(newObj)
end)
end)
makeBtn("666", blue, function()
for i,v in next,workspace:children''do
if(v:IsA'BasePart')then
me=v;
bbg=Instance.new('BillboardGui',me);
bbg.Name='stuf';
bbg.Adornee=me;
bbg.Size=UDim2.new(2.5,0,2.5,0)
tlb=Instance.new'TextLabel';
tlb.Text='666 666 666 666 666 666';
tlb.Font='SourceSansBold';
tlb.FontSize='Size48';
tlb.TextColor3=Color3.new(1,0,0);
tlb.Size=UDim2.new(1.25,0,1.25,0);
tlb.Position=UDim2.new(-0.125,-22,-1.1,0);
tlb.BackgroundTransparency=1;
tlb.Parent=bbg;
end;end;
local s=Instance.new'Sound';
s.Parent=workspace;
s.SoundId='rbxassetid://152840862';
s.Pitch=1;
s.Volume=1;
s.Looped=true;
s:play();
local function xds(dd)
for i,v in next,dd:children''do
if(v:IsA'BasePart')then
v.BrickColor=BrickColor.new'Really black';
v.TopSurface='Smooth';
v.BottomSurface='Smooth';
local sb=Instance.new('SelectionBox',v);
sb.Adornee=v;
sb.Color=BrickColor.new'Really red';
local a=Instance.new('PointLight',v);
a.Color=Color3.new(1,0,0);
a.Range=15;
a.Brightness=5;
local f=Instance.new('Fire',v);
f.Size=19;
f.Heat=22;
end;
game.Lighting.TimeOfDay=0;
game.Lighting.Brightness=0;
game.Lighting.ShadowColor=Color3.new(0,0,0);
game.Lighting.Ambient=Color3.new(1,0,0);
game.Lighting.FogEnd=200;
game.Lighting.FogColor=Color3.new(0,0,0);
if #(v:GetChildren())>0 then
xds(v)
end
end
end
xds(game.Workspace)
end)
makeBtn("Message All", blue, function()
local m = Instance.new("Message", workspace) m.Text = "MANUSIA_HITAM60 HACKED THIS GAME LOL!!!"
task.delay(5, function() m:Destroy() end)
end)
makeBtn("Hint", blue, function()
local h = Instance.new("Hint", workspace) h.Text = "MANUSIA_HITAM60 HACKED THIS FUCKING GAME!!! "
task.delay(9e9, function() h:Destroy() end)
end)
makeBtn("Unanchor All", blue, function()
for _, v in pairs(workspace:GetDescendants()) do if v:IsA("BasePart") then v.Anchored = false end end
end)
makeBtn("Infinite Yield SS", blue, function() 
task.spawn(function()
local sukses, pesanError = pcall(function()
require(7634392335)(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load Infinite Yield SS: " .. pesanError)
end
end)
end)
makeBtn("Blue2Spooky", blue, function()
task.spawn(function()
local sukses, pesanError = pcall(function()
require(137870571533698):load(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load blue2spooky: " .. pesanError)
end
end)
end)
makeBtn("Shutdown", blue, function() for _, v in pairs(Players:GetPlayers()) do v:Kick("THE SERVER SHOUTDOWN BECAUSE MANUSIA_HITAM60!!!") end end)
makeBtn("Kill All", green, function() for _, v in pairs(Players:GetPlayers()) do if v.Character then v.Character:BreakJoints() end end end)
makeBtn("Disco", green, function() 
task.spawn(function() game.Lighting.GlobalShadows = false
while true do local c = Color3.fromHSV(math.random(), 1, 1) game.Lighting.Ambient = c game.Lighting.OutdoorAmbient = c task.wait(0.1) end 
end) 
end)
makeBtn("Explode Rain", green, function() 
task.spawn(function() 
local spawnPos = targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") and targetPlayer.Character.HumanoidRootPart.Position or Vector3.new(0,0,0)
while true do
if targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then spawnPos = targetPlayer.Character.HumanoidRootPart.Position end
local b = Instance.new("Part", workspace) b.Shape = Enum.PartType.Ball b.Size = Vector3.new(3,3,3)
b.Position = spawnPos + Vector3.new(math.random(-499,499), 200, math.random(-499,499))
b.Color = Color3.new(1,0,0) b.Material = Enum.Material.Neon
b.Touched:Connect(function() 
local ex = Instance.new("Explosion", workspace) ex.Position = b.Position ex.BlastRadius = 25
ex.Hit:Connect(function(hit) if hit and hit:IsA("BasePart") and not hit:IsDescendantOf(targetPlayer.Character) then hit.Anchored = false end end)
b:Destroy() 
end) 
task.wait(0.05)
end 
end) 
end)
makeBtn("Sensation", green, function()
task.spawn(function()
local sukses, pesanError = pcall(function()
require(100263845596551)(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load Sensation: " .. pesanError)
end
end)
end)
makeBtn("Stratosphere", green, function()
task.spawn(function()
local sukses, pesanError = pcall(function()
require(6535776034).load(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load Stratosphere: " .. pesanError)
end
end)
end)
makeBtn("Dominant", green, function()
task.spawn(function()
local sukses, pesanError = pcall(function()
require(121425622240385).dominantultimate(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load Dominant: " .. pesanError)
end
end)
end)
makeBtn("Rainbow Ball", green, function() 
task.spawn(function() 
local spawnPos = targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") and targetPlayer.Character.HumanoidRootPart.Position or Vector3.new(0,0,0)
while true do
if targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then spawnPos = targetPlayer.Character.HumanoidRootPart.Position end
local b = Instance.new("Part", workspace) b.Shape = Enum.PartType.Ball b.Position = spawnPos + Vector3.new(math.random(-499,499), 250, math.random(-499,499))
b.Size = Vector3.new(4,4,4) b.Color = Color3.fromHSV(math.random(),1,1) b.Material = Enum.Material.Neon b.CanCollide = true task.wait(0.1)
end 
end) 
end)
makeBtn("Happy Hub", green, function()
task.spawn(function()
local sukses, pesanError = pcall(function()
require(109229360070706):Hload(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load Happy hub: " .. pesanError)
end
end)
end)
makeBtn("SFV3PC", green, function() 
task.spawn(function()
local sukses, pesanError = pcall(function()
require(132592022786319).SFV3PC(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load SFV3PC: " .. pesanError)
end
end)
end)
makeBtn("Rush", green, function()
task.spawn(function()
local sukses, pesanError = pcall(function()
require(104732504345735).jumpscare(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load rush: " .. pesanError)
end
end)
end)
makeBtn("Stigma", black, function() 
task.spawn(function()
local sukses, pesanError  = pcall(function()
require(132938459666886).stigma(targetPlayer.Name)
end)
if not sukses then 
warn("Gagal load Stigma: " .. pesanError)
end
end)
end)
makeBtn("RoLoader", black, function()
task.spawn(function()
local sukses, pesanError = pcall(function()
require(112796675826571).RoLoader(targetPlayer.Name) 
end)
if not sukses then
warn("Gagal load Roloader: " .. pesanError)
end
end)
end)
makeBtn("Fling All", black, function()
for _, v in pairs(Players:GetPlayers()) do
if v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
local hrp = v.Character.HumanoidRootPart
local bv = Instance.new("BodyVelocity", hrp)
bv.Velocity = Vector3.new(0, 9e9, 0)
bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
task.delay(0.2, function() bv:Destroy() end)
end
end
end)
makeBtn("RC7", black, function() 
task.spawn(function()
local sukses, pesanError = pcall(function()
require(12350030542).RC7(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load RC7: " .. pesanError)
end
end)
end)
makeBtn("Dev console", black, function() 
task.spawn(function()
local sukses, pesanError = pcall(function()
require(6016746796):Console(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load Dev console: " .. pesanError)
end
end)
end)
makeBtn("Chat", black, function()
task.spawn(function()
local sukses, pesanError = pcall(function()
require(77886688623397)()
end)
if not sukses then
warn("Gagal load Chat: " .. pesanError)
end
end)
end)
makeBtn("MBG", black, function()
task.spawn(function()
local sukses, pesanError = pcall(function()
require(80602649511365).mbg(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load MBG: " .. pesanError)
end
end)
end)
makeBtn("Morph Gui", black, function()
task.spawn(function()
local sukses, pesanError = pcall(function()
require(124675875890869).load(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load Morph Gui: " .. pesanError)
end
end)
end)
makeBtn("Vakum", black, function()
task.spawn(function()
local sukses, pesanError = pcall(function()
require(4932480712).load(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load Vakum: " .. pesanError)
end
end)
end)
makeBtn("Chat Bypass", black, function()
task.spawn(function()
local sukses, pesanError = pcall(function()
require(7960756691).load(targetPlayer.Name)
end)
if not sukses then
warn("Gagal load Chat Bypass: " .. pesanError)
end
end)
end)
e